# Build outputs
build/
*/build/
.gradle/
.kotlin/
out/
*.class

# The Gradle wrapper jar is the one jar that is committed on purpose; the
# tools/*.jar rule below keeps the downloaded toolchain out.

# Local toolchain and inputs (never commit)
tools/*.jar
tools/jadx*/
*.apk
*.apkm
*.xapk
*.apks
*.keystore
*.jks
local.properties
.env
env.sh

# IDE
.idea/
*.iml
.vscode/
